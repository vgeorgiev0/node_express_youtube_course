const mg = require('./mg');
const alt = require('./alt');
const names = require('./names');
const os = require('./os');
const path = require('./path');

module.exports = {
  mg,
  alt,
  names,
  os,
  path,
};
